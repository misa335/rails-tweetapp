<!DOCTYPE html>
<html>
  <head>
    <title>Tweetie</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="header-logo">
        <% if @current_user %>
          <%= link_to("Tweetie", "/posts/index") %>
        <% else %>
          <%= link_to("Tweetie", "/") %>
        <% end %>
      </div>
      <ul class="header-menus">
        <% if @current_user %>
          <li>
            <%= link_to("login user: #{@current_user.name}", "/users/#{@current_user.id}") %>
          </li>
          <li>
          <%= link_to("Posts", "/posts/index") %>
          </li>
          <li>
          <%= link_to("New Post", "/posts/new") %>
          </li>
          <li>
          <%= link_to("Users", "/users/index") %>
          </li>
          <li>
          <%= link_to("Logout", "/logout", {method: "post"}) %>
          </li>
        <% else %>
          <li>
          <%= link_to("About", "/about") %>
          </li>
          <li>
          <%= link_to("Sign Up", "/signup") %>
          </li>
          <li>
          <%= link_to("Login", "/login") %>
          </li>
        <% end %>
      </ul>
    </header>
    <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
